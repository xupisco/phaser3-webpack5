{
  "name": "phaser",
  "version": "1.0.0",
  "main": "src/game.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run webpack --watch --progress --mode development",
    "build": "yarn run webpack --env production --mode production",
    "serve": "yarn run webpack serve --env development --mode development"
  },
  "devDependencies": {
    "clean-webpack-plugin": "^3.0.0",
    "webpack": "^5.10.0",
    "webpack-cli": "^4.2.0",
    "webpack-dev-server": "^3.11.0"
  }
}
